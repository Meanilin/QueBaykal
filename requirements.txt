# Production dependencies, locked for Docker builds.
# Pinned versions verified against pyproject.toml on 2026-09-07.
# Regenerate via: uv pip compile pyproject.toml -o requirements.txt

aiogram==3.15.0
fastapi==0.118.0
uvicorn[standard]==0.34.0

sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10

redis==5.2.1
# aiogram 3.x ships aiogram.fsm.storage.redis.RedisStorage natively

pydantic-settings==2.7.1
pydantic==2.10.4
structlog==24.4.0
python-dotenv==1.0.1

httpx==0.28.1
tenacity==9.0.0
orjson==3.10.12
